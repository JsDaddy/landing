name: Deploy Landing

on: [push]

jobs:
 deploy:
   runs-on: ubuntu-latest
   steps:
   - name: Deploy
     uses: appleboy/ssh-action@master
     env:
       USERNAME: ${{ secrets.my_username }}
       HOST: ${{ secrets.my_host }}
       KEY: ${{ secrets.SSH_DEPLOYMENT_KEY }}
       SCRIPT: ~/deploy_bash_front.sh
       script_stop: true
